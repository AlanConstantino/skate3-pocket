// Versions match the android-project template shipped with the SDL3 revision
// this app links against, so the org.libsdl.app Java classes compiled from
// that tree are built exactly as their authors test them.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
